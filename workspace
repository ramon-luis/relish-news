


<% is_existing_favorite = @favorites.where(topic_id: topic[:id]).any? %>

<%= button_to "Save to favorites", user_favorites_path(@user.id), class: "btn btn-outline-success", disabled: is_existing_favorite, method: :post, params: { rank: @favorites.length + 1, topic_id: nil, user_id: @user.id } %>
